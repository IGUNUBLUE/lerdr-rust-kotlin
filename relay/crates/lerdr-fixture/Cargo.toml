[package]
name = "lerdr-fixture"
version = "0.0.4"
edition = "2021"
license = "Apache-2.0"
rust-version = "1.85"
description = "Golden fixture suite loader (fixtures/README.md) for the Lerdr vector contract."

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
base64 = "0.23"
hex = "0.4"
thiserror = "2.0"
