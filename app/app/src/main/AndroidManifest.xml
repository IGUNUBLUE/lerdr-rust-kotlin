<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!-- QR pairing is optional — install must still work on camera-less devices. -->
    <uses-feature
        android:name="android.hardware.camera.any"
        android:required="false" />

    <!-- Relays are user-supplied ws:// LAN origins; the socket payload is
         herdr-e2ee-v2 sealed either way, so platform cleartext is safe. -->
    <application
        android:name=".LerdrApp"
        android:icon="@mipmap/ic_launcher"
        android:label="Lerdr"
        android:theme="@style/Theme.Lerdr"
        android:enableOnBackInvokedCallback="true"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Setup links: lerdr://pair?setup=…&label=…&relay=… -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lerdr" android:host="pair" />
            </intent-filter>
            <!-- Session links: lerdr://agent?pane_id=… (notification taps and
                 external shares); lerdr://agents lands on mission control. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="lerdr" android:host="agent" />
                <data android:scheme="lerdr" android:host="agents" />
            </intent-filter>
        </activity>

        <!-- Pins live relay sessions while the app is backgrounded.
             Started by AgentAttentionNotifier on first CONNECTED relay. -->
        <service
            android:name=".notify.RelaySyncService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
    </application>
</manifest>
